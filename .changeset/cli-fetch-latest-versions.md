---
"@kernl-sdk/cli": minor
---

Fetch latest kernl and @kernl-sdk/ai versions from npm at runtime when scaffolding projects, with fallback to baked-in versions. Also updated AI SDK provider versions.
